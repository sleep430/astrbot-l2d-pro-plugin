name: Release

on:
  push:
    branches:
      - main
    paths:
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'dist/**'
      - 'dist-electron/**'
      - 'resources/**'
      - 'scripts/**'
      - 'RELEASE_NOTES_v1.1.md'
      - '.github/workflows/release.yml'
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version, for example 1.1.0'
        required: true
        default: '1.1.0'

permissions:
  contents: write

concurrency:
  group: release-v1-1
  cancel-in-progress: true

jobs:
  windows-release:
    name: Build and publish Windows release
    runs-on: windows-2022

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Resolve release version
        id: version
        shell: pwsh
        run: |
          if ('${{ github.event_name }}' -eq 'workflow_dispatch') {
            $version = '${{ inputs.version }}'
          } else {
            $version = (Get-Content package.json -Raw | ConvertFrom-Json).version
          }

          if ($version.StartsWith('v')) {
            $version = $version.Substring(1)
          }

          if ($version -notmatch '^\d+\.\d+\.\d+(-[0-9A-Za-z.-]+)?$') {
            throw "Invalid version: $version"
          }

          "version=$version" >> $env:GITHUB_OUTPUT
          "tag=v$version" >> $env:GITHUB_OUTPUT
          $notesFile = "RELEASE_NOTES_v$version.md"
          if (-not (Test-Path $notesFile)) { $notesFile = "CHANGELOG.md" }
          "notes_file=$notesFile" >> $env:GITHUB_OUTPUT

      - name: Check whether release exists
        id: existing_release
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ steps.version.outputs.tag }}
        run: |
          gh release view $env:TAG_NAME *> $null
          if ($LASTEXITCODE -eq 0) {
            "exists=true" >> $env:GITHUB_OUTPUT
            Write-Host "Release $env:TAG_NAME exists; assets will be refreshed."
          } else {
            "exists=false" >> $env:GITHUB_OUTPUT
            Write-Host "Release $env:TAG_NAME does not exist yet; it will be created."
          }
          exit 0

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Patch package version for this release
        shell: pwsh
        env:
          RELEASE_VERSION: ${{ steps.version.outputs.version }}
        run: |
          node -e "const fs=require('fs'); const p=JSON.parse(fs.readFileSync('package.json','utf8')); p.version=process.env.RELEASE_VERSION; fs.writeFileSync('package.json', JSON.stringify(p,null,2)+'\n')"

      - name: Build Windows artifacts
        run: pnpm run pack:win

      - name: List artifacts
        shell: pwsh
        run: Get-ChildItem release -File | Sort-Object Name | Format-Table Name, Length

      - name: Create or move tag
        shell: pwsh
        env:
          TAG_NAME: ${{ steps.version.outputs.tag }}
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git tag -f $env:TAG_NAME
          git push origin "refs/tags/$env:TAG_NAME" --force

      - name: Create or refresh GitHub release
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ steps.version.outputs.tag }}
          RELEASE_VERSION: ${{ steps.version.outputs.version }}
          RELEASE_EXISTS: ${{ steps.existing_release.outputs.exists }}
        run: |
          $assets = Get-ChildItem release -File | Where-Object {
            $_.Name -match '\.(exe|yml|blockmap)$'
          } | ForEach-Object { $_.FullName }

          if (-not $assets -or $assets.Count -eq 0) {
            throw 'No release assets were produced.'
          }

          if ($env:RELEASE_EXISTS -eq 'true') {
            gh release edit $env:TAG_NAME --title "L2D Pro v$env:RELEASE_VERSION" --notes-file "${{ steps.version.outputs.notes_file }}" --latest
            gh release upload $env:TAG_NAME $assets --clobber
          } else {
            gh release create $env:TAG_NAME $assets --title "L2D Pro v$env:RELEASE_VERSION" --notes-file "${{ steps.version.outputs.notes_file }}" --latest
          }
